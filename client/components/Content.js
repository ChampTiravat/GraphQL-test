import styled from "styled-components";

/**
 *  @desc A collections of content elements such as headings, paragraphs, images, etc. 
 */

export const PageHeading = styled.h1`
  font-size: 2em;
  font-weight: 400;
  font-family: "Calibri";
  color: #666;
  margin: 0;
  padding: 0;
  display: block;
  text-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
`;
